services:
  app:
    build: .
    container_name: vision-api-app
    ports:
      - "8000:8000"
    environment:
      GOOGLE_APPLICATION_CREDENTIALS: /app/credentials/actask-app-a125d7e12c21.json
    volumes:
      - .:/app
      - ./credentials:/app/credentials:ro
    command: uvicorn main:app --host 0.0.0.0 --port 8000
